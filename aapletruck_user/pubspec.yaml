name: aapletruck_user
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.10.4 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # State management
  flutter_riverpod: ^2.5.1

  # Firebase
  firebase_core: ^2.27.0
  firebase_auth: ^4.17.8
  firebase_analytics: ^10.8.10
  google_sign_in: ^6.2.1

  # Utilities
  flutter_secure_storage: ^9.0.0
  permission_handler: ^11.3.0
  geolocator: ^11.0.0

  # Localization
  flutter_localizations:
    sdk: flutter

  # Icons
  cupertino_icons: ^1.0.8

  # Dev utility (CLI tool, ok here)
  change_app_package_name: ^1.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  # ✅ ONLY ONE flutter SECTION (VERY IMPORTANT)

  uses-material-design: true

  # Localization code generation
  generate: true

  # Assets
  assets:
    - assets/
